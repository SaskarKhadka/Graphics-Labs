let xRadius = parseInt(prompt("Enter x-radius: "));
let yRadius = parseInt(prompt("Enter y-radius: "));
let center = prompt("Enter center: ").split(",");

createEllipseUsingMidpointAlgo(xRadius, yRadius, center);
